# Feedback

## What belongs here

StatusPill, BadgeChip, Stepper, Tabs, EmptyState (routes to EdgeStates), Skeleton, Spinner, AgingTint.

## What does not belong here

Feature-specific components. Anything that reaches into a specific module's state.

## Notes

Every stepper renders as circles on a continuous track — never bordered rectangles.
