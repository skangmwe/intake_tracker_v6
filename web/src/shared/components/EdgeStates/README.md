# EdgeStates

## What belongs here

NoAccessPage (S40), EmptyListZeroData (S41), EmptyListFilteredToZero (S42).

## What does not belong here

Feature-specific components. Anything that reaches into a specific module's state.

## Notes

NoAccessPage NEVER reveals record existence. Filtered-to-zero uses bordered card, not pale-fill.
