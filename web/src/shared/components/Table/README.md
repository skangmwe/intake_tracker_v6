# Table

## What belongs here

TableShell (in-list scroll, sticky headers, resizable columns), SavedViewPicker, ViewBar, FilterFunnel — the canonical items-grid pattern.

## What does not belong here

Feature-specific components. Anything that reaches into a specific module's state.

## Notes

Every table shell sets data-ds="table"; saved-view picker data-ds="saved-view-picker".
