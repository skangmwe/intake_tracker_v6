# Architectural findings ledger

Append-only. Every architectural finding that has surfaced, with the developer's decision. Unit-test findings use the `unit-test/` match-key prefix.

| Match Key | First Seen | Status | Decided At | Rule | File | Reason |
|-----------|------------|--------|------------|------|------|--------|
| unit-test/web::web-testing.md#coverage-floor | 2026-07-06 | Accepted | 2026-07-06 | coverage-floor | web (global) | Pre-existing debt from slices 1–14 (slices 15 & 16 shipped at the same 76%/77% level, accepted as pre-existing). Slice-17 files sit above the global average, so they can only raise the global. Raising the whole project to floor is cross-slice debt, out of scope for slice 17. Developer accepted + authorized ship. |
| api/Api/../usp_GetBridgeForRecord.sql::correctness#conditional-result-set | 2026-07-17 | Applied | 2026-07-17 | ef-fromsql-result-shape | database/procedures/requests/usp_GetBridgeForRecord.sql | CRITICAL: GET /requests/{id} 500 for every non-escalated record (proc early-RETURN → no result set → EF FromSql<BridgeRow> "required column AiFieldValues not present"). Pre-existing/cross-slice, surfaced by the Iteration-2 design-fidelity stand-up. **User authorized the out-of-slice fix.** Fixed to always emit the BridgeRow shape (0 rows via WHERE guard); verified 200 on non-escalated + escalated. |
| design-fidelity//requests/:recordId::S4/watchers-notify-model | 2026-07-17 | Applied | 2026-07-17 | design-fidelity-web.md#visual-drift | web/src/features/watchers/WatchersCard.tsx | RECONCILED TO PROTOTYPE (user: "prototype should win" — prototype is current, not stale). Relabelled "Notify me about" → "Notify watchers about"; ungated the five preference toggles (now always visible, not behind "Watch this record") + added the "Active alerts" section. Full-stack ungate: new `usp_GetMyWatcherPreferences` proc + `MyWatcherPreferencesRow` entity + `WatcherListDto.MyPreferences` + `collaboration.ts` `myPreferences` so a non-watcher's prefs read/persist. Tests updated; API 577/577, web 1149/1149. |
| design-fidelity//requests/:recordId::S4/status-control | 2026-07-17 | Applied | 2026-07-17 | design-fidelity-web.md#visual-drift | web/src/features/requests/components/RecordDetailPage.tsx | RECONCILED TO PROTOTYPE: relabelled the tri-state control "Status" → "Status override" (prototype's field label). Option vocabulary kept as In progress/On hold/Abandoned (spec-correct + consistent with StatusHoldPill; the prototype's "Active" wording not propagated to avoid internal inconsistency). |
| design-fidelity//requests::S2/status-hold-pill | 2026-07-17 | Applied | 2026-07-17 | design-fidelity-web.md#added-element | web/src/features/requests/components/RequestsListPage.tsx | RECONCILED TO PROTOTYPE (user chose "remove it"): removed the per-row StatusHoldPill + its import from the Requests list — the prototype list shows no hold pill on rows. The pill remains on the record-detail header where the prototype does show it. |
| design-fidelity//requests/:recordId::S4/record-detail-structure | 2026-07-17 | Deferred | 2026-07-17 | design-fidelity-web.md#visual-drift | web/src/features/requests/components/RecordDetailPage.tsx | CROSS-SLICE (not slice-26): dark-navy vs pale-blue section-header pills, Status-tab SUBMITTED/LIFECYCLE/STATUS-CATEGORY meta row + SLA block + STATUS HISTORY & REACTIVATION TRAIL absent in build, Attachments↔Tasks&gates tab order swap, and the prototype's "Active alerts" section (live-alerts feed) not built. Record detail was built in slices 5/9/21 and has shipped through every prior gate (which never ran the design-fidelity stand-up). Reconcile as separate cross-slice design-debt work, not a slice-26 blocker. |
| design-fidelity/GATE::design-fidelity-web.md#render-failed | 2026-07-17 | Deferred | 2026-07-17 | design-fidelity-web.md#render-failed | artifacts/docs/design/project (DCLogic prototype) | The per-component (type,ordinal) computed-diff manifest is unobtainable for this Claude Design DCLogic single-file prototype: enumerate-prototype-components.mjs → 0 components; render-states.mjs cannot click-navigate the no-deep-link SPA. Build side works. RESOLVED for ship via the validator's built-in `component_coverage:"waived"` path — screen-level render-and-compare ran for every in-scope screen (both shots recorded in reviews/shots + reviews/.last-clean-run.json). Underlying per-component tooling gap stays Deferred: needs a design-fidelity path for the DCLogic format (or a re-export the hooks can enumerate). |
| design-fidelity//requests::S2/view-mode-toggle | 2026-07-18 | Deferred | 2026-07-18 | design-fidelity-web.md#visual-drift | web/src/features/requests/components/RequestsListPage.tsx | CROSS-SLICE (not record-status-hold): the Requests-list toolbar carries a Table/Board/Timeline/Agenda view-mode segmented control absent from the prototype's Requests screen (prototype view vocabulary is Table/Board/Calendar). Built in the advanced-views slice (not in this worktree's dev base); shipped through its own gate. Surfaced by the render-and-compare stand-up. Reconcile as advanced-views design-debt, not a record-status-hold blocker. |
| design-fidelity//requests/new::S3/intake-field-order | 2026-07-18 | Deferred | 2026-07-18 | design-fidelity-web.md#visual-drift | web/src/features/requests/components/IntakeFormPage.tsx | CROSS-SLICE (not record-status-hold): intake-form Intake card leads with 'Request type' + adds Workflow Details / Business Owner / Timing, where the prototype leads with 'Request name' and places Lifecycle lower. Built in the requests-core slice; shipped through its own gate. Shell + Similar-requests panel + numbered section-card pattern match. Reconcile as requests-core design-debt, not a record-status-hold blocker. |
| design-fidelity//admin/lifecycle::S31/lifecycle-picker-shape | 2026-07-18 | Deferred | 2026-07-18 | design-fidelity-web.md#visual-drift | web/src/features/lifecycle/components/LifecyclePage.tsx | CROSS-SLICE (not record-status-hold): Lifecycle & gates uses a card-picker + Workspace selector + 'Saved' indicator where the prototype uses a native <select>; settings sub-nav label reads 'Manage announcements' vs the prototype's 'Announcements'. Built in the lifecycle-admin slice; shipped through its own gate. Workspace-settings shell + sub-nav + Lifecycles card + stages detail match. Reconcile as lifecycle-admin design-debt, not a record-status-hold blocker. |
