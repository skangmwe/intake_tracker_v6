# fix-s29-members-list-surface-c3f6e33 — remediations applied

## Iteration 1
Mechanical fixes applied during authoring (pre-review), no review-surfaced remediations outstanding:
- Phase 1 (code-quality): keyed TableShell cells + string level cell (react/jsx-key); `tabIndex={-1}` on the menu container (interactive-supports-focus); removed unused imports (`UserId` in MembersPanel/MembersTable). Re-ran affected tests green after each.
