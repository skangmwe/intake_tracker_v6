// Fluent builder for firm error codes. Consumers write .WithCode(ErrorCode).WithDetail(string).AsResponse(). Filled in slice 2.

namespace McDermott.AiTracker.Api.Shared.Errors;

// Placeholder — see shared-inventory.md 'problem-response' entry.
