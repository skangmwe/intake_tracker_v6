# Auth

## What belongs here

AccessGuard — RequireWorkspaceMembership, RequirePlatformAdmin, RequireOwnership. Attribute-based authorization helpers.

## What does not belong here

SSO token validation — Microsoft.Identity.Web handles that. AAD app-registration content — that's deployment configuration.

_Traced from `/artifacts/docs/dev/architecture/shared-inventory.md`._
