// Access guard — RequireWorkspaceMembership / RequirePlatformAdmin / RequireOwnership. Returns 403 (never 404) on ownership violations (api-error-handling.md). Slice 2 wires the real implementation.

namespace McDermott.AiTracker.Api.Shared.Auth;

// Placeholder — see shared-inventory.md 'access-guard' entry.
