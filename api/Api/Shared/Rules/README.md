# Rules

## What belongs here

The condition engine — Show/Hide/Require/ProduceValue rule evaluator. Depth- and cycle-checked at save time.

## What does not belong here

Field definitions themselves — those live in the Fields module. The engine consumes the definitions.

_Traced from `/artifacts/docs/dev/architecture/shared-inventory.md`._
